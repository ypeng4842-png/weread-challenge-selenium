name: 微信读书自动打卡阅读 (双容器矩阵版)

on:
  workflow_dispatch:
  schedule:
    # 每天北京时间早上 9 点准时进入列队 (UTC 01:00)
    - cron: '0 1 * * *'

jobs:
  run-challenge:
    runs-on: ubuntu-latest
    # 物理级熔断：最大运行 120 分钟（2小时）后强制安全下线，防风控死循环
    timeout-minutes: 120
    steps:
      - name: 检出代码
        uses: actions/checkout@v3

      - name: 启动双容器阵列
        env:
          # 从保险箱直接提取原始数据
          RAW_COOKIE: ${{ secrets.WEREAD_COOKIE }}
        run: |
          # 使用单引号强行锁定字符串，防止环境变量被转义
          echo "WEREAD_COOKIE='${RAW_COOKIE}'" > .env
          echo "WEREAD_SELECTION=-1" >> .env
          
          # 启动阵列
          docker compose up --build --abort-on-container-exit
